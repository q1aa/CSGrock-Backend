namespace CSGrock.CSGrockLogic.Utils.Enums
{
    public class RequestEnum
    {
        //create a new enum for the request types
        public enum RequestType
        {
            GET,
            POST,
            PUT,
            DELETE,
            PATCH,
            HEAD,
            OPTIONS,
        }
    }
}
